# Make RVM run in shell mode.
# Presumably either Ruby or Rails added this to my .bash_profile
# a while back.
# TODO: Confirm this does what I think it does.
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
